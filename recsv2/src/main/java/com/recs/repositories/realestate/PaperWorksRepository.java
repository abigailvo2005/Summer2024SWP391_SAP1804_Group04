package com.recs.repositories.realestate;

import com.recs.models.entities.realestate.PaperWorks;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaperWorksRepository extends JpaRepository<PaperWorks, String> {
}
